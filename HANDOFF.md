# Agent 快速交接

> 更新：2026-09-19。进度：设计文档已建立，业务实现未开始。

## 1. 五分钟了解现状

这是“未来战争”竞赛机器人，使用 Rust 2024。仓库当前 `src/main.rs` 只输出 `Hello, world!`，`Cargo.toml` 没有依赖；没有 HTTP 服务、AI、测试套件或 `run.sh`。设计文件里的模块树、接口示意和性能数字是计划与目标。

用户要求设计 RTS 风格的战略层、任务层、战术层、个体层和世界模型，并进一步细化每层实现工作、每个 FSM 状态的行为、事件上报和状态转移。本次仍是文档设计任务，没有开始业务实现。

最新补充是可直接指导编码的行为规格：`BEHAVIOR.md` 定义 78 个状态、三角色/三武器策略、A01–A12 算法、G01–G16 守卫、确定性排序、策略参数和 BH01–BH18 验收；PLAN 第 4 节把它们拆为 B01–B10 工作包。此前 `INTERFACES.md` 的 IF01–IF16 继续作为契约基线，本次增加行为输入/输出和转移轨迹类型；所有模块尚未实现。

建议阅读顺序：

1. [AGENTS.md](AGENTS.md)：仓库约定和文档职责。
2. [AGENT_CONTEXT.md](AGENT_CONTEXT.md)：已确认需求、证据与当前工作记录。
3. [DESIGN.md](DESIGN.md)：4.4–4.5 为世界模型职责/状态，第 6 节为四层状态行为和转移表，第 7 节为事件/上报契约，10.3–10.5 为认知任务子状态机，11.1 为必测轨迹；第 12 节仍维护未决项。
4. [INTERFACES.md](INTERFACES.md)：先读第 2 节基础类型/所有权、第 4 节回合事务、第 7 节层级契约，再读对应 IF 接口。
5. [BEHAVIOR.md](BEHAVIOR.md)：先读参数及 A01–A03，再读角色、目标算法和对应状态表；第 9 节给出可做 fixture 的数值用例。
6. [PLAN.md](PLAN.md)：P0–P7 阶段与 B01–B10 具体实现包、依赖和验收门槛。
7. [DECISION.md](DECISION.md)：重大设计选择与重新评估条件，新增 D19–D21 解释行为基线。

## 2. 不可遗漏的设计约束

- 世界模型保存确认事实、历史记忆和预测；决策层只读快照，不能预先扣除真实金币或假设动作已成功。
- 目标向下传递，上报向上传递；调度器分阶段投递事件，避免 FSM 递归回调和无界重规划。
- 全部建议经统一动作仲裁。攻击以武器 ID 为响应 key，预约武器和操控角色，不得同时给该角色另一个动作。
- 只有三名角色，武器总数最多三座；角色匹配使用有预算的小规模枚举。
- 寻路八方向、切比雪夫距离；基地为左上角定位的 2×2；互动目标通常是物体旁边的可站立格。
- 5 秒内须返回 HTTP 响应。LLM/沙盒通过比赛协议提交并在后续回合取结果，不在 HTTP 中等待，也不把任务命令放到本机执行。
- 自进化任务离开邻域/死亡会终止；等待认知结果期间保持开拓者站位，其他角色继续执行任务。
- 非任务期间 LLM 每日三次；任务期豁免必须在观测确认接取后启用。作业、预约和缓存都要防重复推进。
- 宝藏合法尝试即消耗用品，即使地点或时间错误；结果码 2 不能区分这两种原因。
- `ActionCommitted` 才使个体等待回执；本地未获选建议不进入等待，裁判接受也不等于实际目标完成。
- 报告区分 Step/Plan/Mission 作用域；局部到位或一次攻击不能完成整夜防守。取消增加 generation，但旧动作实际效果仍需对账。
- 内部取消挑战不代表裁判已经结束题目；迟到模型结果需经过作业墓碑和归属检查，不能绑定新任务。
- 跨层消息带完整 OwnerPath，父任务取消使子建议失效；各层只改自身草稿，资源账本仅由 ResourceCoordinator 修改。
- 观测事实幂等归并与规划事务提交分开；校验、编码及草稿提交回调成功后，才能提交等待状态和响应缓存。
- 对比分数/动作的严格回放使用固定工作量预算；实时墙钟降级可能随负载产生不同截断点。
- BEHAVIOR 参数分 R（规则）/P（策略初值）/U（未确认）；风险阈值、三炮组合、返防余量及成功率先验尚未实战调优。
- W1/W2 只是两名工人的并列偏好，不是硬分工；全队匹配先保全/必要防御，再比较收益，个人背包不可互借。
- 所有等待必须有具体唤醒条件、截止和未知结果处理；移动失败和重规划按真实无进展累计，不能靠改 PlanId 清零。

## 3. 下一次开始实现时

先执行 PLAN 的 P0，再实现 P1 的最小协议服务；行为落地按第 4 节 B01→B10 及其依赖，先参数/守卫/世界/几何，再动作、任务和高层策略。优先确认地图建造掩码、昼夜编号、任务点邻域、HTTP 路径与运行环境；问题编号和临时方案统一维护在 DESIGN 第 12 节。

在正式规则未齐全时，可以实现 DTO、世界索引、FSM、回放和预算；不要自行把 Demo 的建造环当成合法地图。P1 验收目标是启动、合法空响应、重复请求幂等和有界降级，然后再进入经济/防守逻辑。

常用检查命令：

```bash
git status --short
cat Cargo.toml
cat src/main.rs
cargo build
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test
```

这些是开发命令，不表示本轮已运行编译或测试。未来增加业务代码后必须执行对应验收；不要把空测试套件通过写成框架已验证。

## 4. 资料陷阱与验证范围

- `docs/docs/response.txt` 是动作变体目录，重复角色 key 是文档示意，另有 JSON 语法问题；拆成独立有效 fixture，保留原文。
- `request.txt` 缺少部分正式字段，射程/物品大小写也有差异；兼容字段缺失不等于可以忽略冷却等规则。
- Demo 只作参考，包含简单昼夜逻辑和 A*；其有限战术、地图推导与字段默认值没有裁判权威性。
- 《编译运行环境说明》不在现有资料内；尚未验证部署平台、比赛启动调用和完整对局。
- 本轮验证范围是文档完整性、内部链接、表格结构、78 个状态覆盖、接口/转移交叉核对及数值算例核算，没有运行机器人性能或比赛测试。FSM01–FSM16、API01–API15、BH01–BH18 都是未来测试规格。

## 5. 工作区与交接维护

本轮开始时工作区已有 `.gitignore` 修改，以及未跟踪的 Cargo 文件、源码、比赛资料和 `.DS_Store`；不要清理、回滚或把这些状态当成本轮产生。`b2ca7bf`（开发规范）和 `30e7c90`（架构/接口）此前已提交推送；本次新增 BEHAVIOR 并同步其余七份文档，不包含源码或原始比赛资料。下一次操作前重新检查 Git 状态。

每次结束工作时更新此文档的实际阶段、已通过检查、阻塞项和下一步；在 `AGENT_CONTEXT.md` 留简明事实记录。架构变化同步 DESIGN 和 DECISION，计划完成项只按实际验收勾选。比赛运行时的认知记忆不写入这些开发文档。
