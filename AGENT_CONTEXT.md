# Agent 上下文记录

> 用途：保存开发过程中的用户要求、仓库事实、验证证据和工作进度，帮助后续大模型继续工作。记录可检查的结论与简要理由，不保存逐字聊天、隐私、密钥或未经验证的推断。
> 更新：2026-09-19。此文件不是比赛中发送给 LLM 的 prompt，也不是运行时长期记忆库。

## 1. 当前用户需求

用户要求阅读 `docs` 下任务书，规划核心框架，并交付：

- `DESIGN.md`：项目结构、算法与架构。
- `HANDOFF.md`：Agent 快速上手和交接。
- `PLAN.md`：实施步骤与验收计划。
- `AGENT_CONTEXT.md`：开发中的上下文积累。
- `DECISION.md`：重大方案决策。
- 在既有 `AGENTS.md` 中补充上述文档用途。

明确架构偏好：参考红色警戒等 RTS AI，采用战略、任务、战术、个体与世界模型分层；各层用有限状态机管理行为，事件与上报触发转移；世界模型管理资源、建筑、人物和场上事件。补充组件须服务于比赛规则和运行可靠性。

后续细化要求：在 DESIGN 中明确每层需要实现哪些组件/工作、每个 FSM 状态的动作行为、事件上报内容，以及状态间的触发事件、guard 和转移规则。设计规格已完成；当前正按计划逐步落地。

此前补充要求：将程序各模块、各层级之间使用的接口定义到文档中；INTERFACES 已作为契约基线。

最新要求：按照计划搭建框架并逐步开发。已完成首个可运行切片；完整逐状态规格仍以 BEHAVIOR 为目标，PLAN 用 P0–P7/B01–B10 跟踪剩余工作。

用户明确要求搭建和开发，故本轮包括 Rust 源码、测试、启动脚本和文档同步。此前“AGENTS.md 已存在则不覆盖”只约束首次创建；后续用户已明确授权完善该文件。

## 2. 仓库与证据快照

| 项目 | 已确认事实 |
| --- | --- |
| 语言/构建 | Rust 2024，Cargo package `technetium-battle-bot`；运行依赖 `serde`、`serde_json`，`run.sh` 锁定 release 构建 |
| 源码 | 首个可运行切片已有 HTTP、协议、世界差分、部分策略/仲裁及独立测试；完整目标未实现，见 HANDOFF |
| 规则来源 | `docs/docs/任务书.md`，506 行，v1.0，2026-09-09 |
| 协议来源 | `docs/docs/接口文档.md`，246 行，v1.0，2026-09-09 |
| 示例 | `request.txt`、`response.txt` 已阅读；后者是动作目录，不能直接当有效单回合 JSON |
| Demo | 通过 `tar -xOf` 阅读 `brain.py`、`grid.py`、`protocol.py`、`server.py`、`main3.py`；未运行、未解包覆盖仓库 |
| Demo 线索 | HTTP POST、空指令兜底、1 起始回合、2×2 基地、八方向 A*；均需区分规范与实现假设 |
| Git 约定 | 设计基线已推送至 `1cbe17f`；当前 main 跟踪 origin/main，代码变更需按 AGENTS 提交并推送 |
| 环境缺口 | 资料引用的《编译运行环境说明》未提供；未联调官方判题器 |

任务书关联图片中的完整建造坐标未验证，不能声称已经确认地图掩码；曾尝试访问远程图但未取得可用坐标。测试掩码为合成数据。依赖已通过 Cargo 下载并构建；未运行 Demo。

## 3. 已形成的方案摘要

采用单 Rust package 的模块化结构，以确定性回合调度器驱动四层 FSM。世界分为观测、记忆、预测；上层发目标与预算，下层发状态与风险报告；每回合分上行对账和下行规划，统一预约资源、验证动作并编译协议。

新增支撑组件：规则与地图服务、任务 DAG/租约、命令仲裁、预算与兜底、重复请求缓存、跨回合认知作业、SOP 记忆、决策日志和回放。首版使用八方向 A*、三角色小规模分配、规则约束内的效用评分与有限候选火力搜索。

BEHAVIOR 细化世界4态、战略7态、任务10态、战术11态、个体7态、挑战14态、认知作业8态、新闻6态、宝藏11态，共78态。A01–A12/G01–G16 统一推进、风险、返防、分配、经济、建设、火力、结果确认与恢复；W1/W2偏好服从全局分配，P负责挑战/宝藏并可补防。P类阈值、流程耗时和成功率先验是未调优策略，不是裁判规则。实现顺序见 PLAN 的 B01–B10，验收增加 BH01–BH18。

LLM 服务于新闻推理、宝藏线索和自进化任务，通过比赛的 `prompt` 与下一回合 `llmResp` 交互。沙盒同样通过协议交互。实时移动和射击的核心闭环由确定性逻辑完成。具体决策及代价见 [DECISION.md](DECISION.md)。

细化版设计已补充世界生命周期、各层状态行为/转移矩阵、10 类任务流程、类型化事件/上报目录，以及挑战、认知作业、新闻和宝藏子状态机。共用约束包括分阶段有限推进、保存未消费证据、报告作用域、取消代次和终止作业墓碑；动作提出、提交和效果确认分开处理。

目标接口基线为 IF01–IF16，统一 TurnStamp/OwnerPath、任务规范与私有记录、意图/建议/已提交动作、类型化事件、资源租约与认知作业。当前实现仅有简化的 Session 草稿提交、WorldView 与 Arbiter；ResourceCoordinator、共享 decision 模块、正式报告路由与预测仓尚未实现。

## 4. 重要事实与待确认项

已确认：白天 70/夜晚 60 回合，最大 1300 回合；武器总数最多 3；控制者只能同时操控一炮；任务离开区域会结束；非任务期 LLM 每日 3 次；合法失败献祭也消耗用品；5 秒响应、累计 5 次异常停调度。

待确认统一编号：U01 建造掩码、U02 回合起点/复活边界、U03 任务交互区域、U04 HTTP/空字段契约、U05 示例字段差异、U06 弹道/伤害细节、U07 会话与结果关联、U08 操炮动作槽、U09 编译运行环境。完整描述及降级策略只在 [DESIGN.md](DESIGN.md) 第 12 节维护，避免多个文件各自改变规则。

## 5. 工作记录

### 2026-09-19：建立设计基线

- 阅读完整任务书、接口文档和示例，检查 Demo 的运行、协议、寻路及策略实现。
- 新建五份文档并给 `AGENTS.md` 增加职责导航；没有修改 Cargo 配置、源码、原始规则和 Demo。
- 区分当前代码、目标结构、性能目标和规则假设；将未决项放入 P0/P1 验收门槛。
- 验证：文档存在、标题、相对链接与关键规则交叉检查；本轮没有代码测试或性能测量。
- 下一步：若用户继续要求实现，开始 PLAN P0，再实施 P1；无需重新规划已确定的五层结构。

### 2026-09-19：细化状态机与事件契约

- 按用户追加要求扩充 DESIGN 4.4–4.5、第 6–7 节及 10.3–10.5，保留原第 12 节未决项编号。
- 给世界、战略、任务、战术、个体和认知流程补充状态行为与转移；定义进入/退出、抢占、恢复、截止、作用域和资源释放语义。
- 补齐事件生产者、payload、路由和优先级；区分本地拒绝、动作提交、裁判回执与效果证据；明确迟到结果不能应用到新任务。
- 增加 DESIGN 11.1 的 FSM01–FSM16 未来验收轨迹，同步 PLAN/HANDOFF，并在 DECISION 追加 D13–D14。
- 验证范围：文档结构、表格、链接、状态/事件名称交叉核对；没有实现或执行 FSM 测试，源码及原始比赛资料未修改。
- 下一步：按 P0/P1 开始后续实现；FSM 实施以本次细化表格和验收轨迹为准。

### 2026-09-19：定义模块和层级接口

- 新建 INTERFACES，定义 IF01–IF16 的调用签名、数据字段、生产/消费方向、所有权、错误、版本与生命周期，并增加单回合时序图及 API01–API15 验收规格。
- 移除 DESIGN 中重复的早期结构草图，改为引用接口契约；补充 decision 模块、预约唯一写入者、事务准备/提交及确定性预算边界。
- 同步 AGENTS 文档导航、PLAN、HANDOFF，并追加 DECISION D15–D18；源码、Cargo 配置和原始比赛资料不变。
- 延续用户开发要求：代码文件至多 500 行，函数至多 50 行，分支循环合计至多 6；数字常量化，业务/测试分文件，完成后提交推送，框架变更同步文档。详细统计口径以 AGENTS 为准。
- 验证范围是文档链接、结构、接口编号/字段和跨文档一致性；契约与 FSM 测试均尚未实现，不声称 API 已可编译或联调通过。
- 下一步：后续实现从 P0/P1 开始，按 IF01–IF03 落地协议与回合事务，之后按 PLAN 接入其他接口。

### 2026-09-19：细化角色、逐状态算法与实现工作包

- 重读任务书中的角色能力、矿产/物品/战斗、任务结束和宝藏语义，沿用 U01–U09 的验证门槛。
- 新建 BEHAVIOR，逐状态指定输入计算、建议输出、守卫切换、等待/恢复和终态清理；加入参数表、优先级、风险/期限公式、全队匹配、采售、建造、联合火力及数值用例。
- 明确 W1/W2/P 与三武器职责；结果未知不退款、不盲重试，挑战内部取消不伪造裁判结束，宝藏证据质量不冒充统计概率。
- INTERFACES 增加行为值对象和 TransitionRuleId/BehaviorTrace；DESIGN 同步重规划计数及等待边；PLAN 增加 B01–B10 工作包，DECISION 追加 D19–D21，AGENTS/HANDOFF 更新导航。
- 检查范围：链接、标题/代码围栏/表格结构、78态和A/G/BH编号覆盖、跨文档规则一致性、算例计算和Git差异；未实现或运行业务测试、性能测试、官方对局。
- 下一步：授权实现时从 P0/P1 开始，按 B01–B10 依赖逐包开发，先把小算例转为独立测试 fixture；不要把文档完整当实现完成。

### 2026-09-19：首个可运行框架切片

- 用户明确要求开始搭建和逐步开发。新增 Cargo 依赖、`src/` 模块、`run.sh`、独立 `tests/` 和协议 fixture；保留原始 `docs/` 与用户已有工作区改动。
- 实现标准库 HTTP 服务、类型化协议/重复 key 拒绝、回合缓存与草稿提交、观测差分/有限记忆、阶段选择、基础采售/建炮/接题/开火、八向 A* 和动作仲裁。建造只允许从外部确认的掩码读取，未提供时禁用；合成掩码只用于测试。
- 首阶段修正基地风险/目标排序使用完整 2×2 占地，且 `llmResp` 仅能在紧随发送 prompt 的下一回合用于当前题目。
- 现有状态 enum 不代表目标 78 态已实现；高级任务 DAG、事件上报、回执、沙盒/SOP、新闻/宝藏与官方联调仍待开发。具体差距见 HANDOFF/DESIGN 2.1。
- 本地完成格式、Clippy、独立回归测试、锁定 release 构建及 HTTP 冒烟；完整命令与最新数量以本阶段最终验证记录为准。尚无官方整场对局或性能测量。
- 下一步：按 PLAN 优先完成 P0 官方地图/环境依据，再补齐 P1 事务与异常、P2 状态/报告闭环；不可把当前基础策略视为完整比赛 AI。

## 6. 后续记录规范

每次记录应包含日期、用户新增约束、实际改动、执行过的检查及结果、未完成项和下一步。事实改变时更新上方摘要，重要架构变化追加 DECISION 条目；不要仅追加日志而留下过时的当前状态。

开发上下文不宣称测试通过或功能完成，除非有对应命令/回放证据。比赛新闻、模型回答和沙盒输出属于运行时数据，应由未来 `cognition/` 与回放存储管理。
