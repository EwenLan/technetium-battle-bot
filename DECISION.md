# 重大设计决策

> 更新：2026-09-21。以下“采纳”表示设计已选定，实际实现范围见 [HANDOFF.md](HANDOFF.md)；“暂定”需要裁判/环境验证。记录结论、依据、代价与调整条件。

## D01：分层 FSM 与显式职责边界

**状态：采纳。** 战略、任务、战术、个体各自拥有 FSM，通过世界模型读数据，通过意图和报告协作。对应用户明确的 RTS 架构要求。

FSM 管理跨回合阶段，效用评分在状态允许的策略中做选择。相比单一巨型状态机，这使采集、任务求解和防守可以各自推进；代价是需要明确契约与事件时序。暂不引入复杂行为树/GOAP，引入条件是具体状态逻辑出现无法清晰维护的组合规划需求。

## D02：单包模块化，显式枚举状态

**状态：采纳。** 当前只有三名角色、最多三座武器，初版单 Cargo package 足以容纳框架。以 Rust enum 表达状态与动作，使用索引表管理实体；不提前建立完整 ECS、脚本运行时或动态插件系统。

这降低接口和部署成本。未来只有在模块确需独立发布、构建或性能隔离时才拆 crate；无需为分层本身增加进程边界。

## D03：世界事实、记忆、预测分开

**状态：采纳。** 每回合请求是确认事实来源；计划支出、敌人最后位置和新闻预测放在各自结构中，带回合、来源和有效期。

依据是局部视野、动态矿点、失败动作和跨日新闻。代价是需要做对账与失效处理，收益是避免把“已发出命令”当成“已成功执行”，也避免敌人离开视野后被错误删除为死亡。

## D04：确定性分阶段事件与上报

**状态：采纳。** 使用进程内、有序、有界的事件队列；上行对账/汇总后再下行规划。普通下行报告下一回合消费，紧急情况最多进行一次预算内重规划。

不采用同步递归回调或通用异步消息代理，避免状态切换次序难以回放及事件风暴。代价是部分非紧急消息延后一回合，关键世界事件则在当前回合规划前可见。

## D05：全局动作仲裁与共享资源预约

**状态：采纳。** 个体只生成动作建议，最终输出统一校验。攻击占用武器及控制者两个槽，金币、背包物品与目的格也统一预约。

依据是每角色每回合一个动作、武器手动操控和多人共享金币。仅用响应 Map 去重不能检测“武器 key 对应的攻击”与“角色 key 对应的移动”冲突。保守禁止控制者另行动作；若官方明确允许更宽松行为，再经测试修改此约束。

## D06：任务契约、租约与小规模枚举分配

**状态：采纳。** 每个任务定义完成证据、能力需求、截止时间和抢占规则，使用 DAG 表达采集/出售/购买/升级依赖；三角色的候选分配采用剪枝枚举。

这使任务中断和资源回收可追踪。通用经济任务可挂起，自进化任务离开邻域即结束，必须取消而不是假装可续接。候选规模增长导致预算紧张时，先限候选数量，再评估更复杂的匹配算法。

## D07：八方向 A* 与保守移动预约

**状态：采纳。** 使用切比雪夫启发式，允许规则规定的对角线穿行，以合法交互邻域为终点；同回合禁止己方目标争夺与互换。

初版不进入其他角色当前占据的格子，即使对方计划离开，以降低同时结算的不确定性。代价是狭窄区域效率较低；获得裁判碰撞用例后再扩展带时间维度的协同寻路。

## D08：存活风险约束下提升积分

**状态：采纳。** 优先稳定性与避免基地过早被毁，再按任务效率、击杀和生存收益分配资源。按路径时间提前返防，使用不同紧急进入/退出阈值控制策略抖动。

依据是先后被毁判负、递增生存积分和任务快速完成奖励。不能把“生存优先”实现为所有角色一直守家；开拓者任务收益纳入机会成本。战略权重是待调参数，必须用换边、相同场景比较，不能声称已有最优配兵。

## D09：LLM 与沙盒使用跨回合作业

**状态：采纳。** 通过比赛协议发送 `prompt/executeCmd`，下一回合处理结果；实时行动不依赖即时 LLM 响应。每通道最多一个在途作业，以回合和任务代次关联，过期结果隔离。

依据是官方接口、每日额度与 HTTP 限时。初版优先确定性规则控制，LLM 负责非结构化理解与任务求解。代价是步骤之间至少存在回合延迟，因此需要截止时间预算、部分答案和 SOP 复用。协议没有可靠关联信息时不强行接受迟到结果。

## D10：规则版本化，未知条件保守关闭

**状态：采纳；具体缺失规则暂定。** 正式文字作为规则依据，当前请求作为状态依据；示例和 Demo 用于兼容测试与线索。动态价格/射程使用观测，缺失信息保留未知状态。

建造区域原先在未知时采用保守禁用，现已由 D24 的确认规则替代；其他未知规则仍不从 Demo 猜补。Demo 的回合 1 起始作为暂定设置，但不是规范证据。代价是资料缺失时功能可能不完整；通过 DESIGN U02–U10 管理验证门槛。

## D11：有界单回合事务、幂等与降级

**状态：采纳；预算数字暂定。** 单会话串行提交，同请求去重；规划预算检查与 generation 检查共同阻止超时旧任务覆盖状态。目标常规 50 ms、软限 100 ms、硬限 500 ms，均待实测。

依据是官方 5 秒响应窗口、异常累计 5 次后停调度及进程退出不重启。失败时输出已验证子集或空动作，而不拼凑不完整命令。空动作契约仍须联调；单进程会话边界需确认换边重启方式。未来只有测得吞吐瓶颈才引入并发规划。

## D12：测试回放先行，交接文档分工

**状态：采纳。** 以真实协议 fixture、FSM/规则测试和连续回合回放作为验证基础；性能、积分改进必须带配置、环境与样本记录。回放不是完整模拟裁判，未验证的结算不能凭模拟结果宣布正确。

文档分别维护稳定设计、操作交接、计划、开发上下文和重大决策，`AGENTS.md` 提供导航。运行时认知记忆另设存储。代价是变更时需同步少量摘要，收益是新 Agent 能分清“已决定”“已实现”“已验证”。

## D13：动作阶段与任务完成作用域显式分离

**状态：采纳，补充 D04–D06。** `ActionProposal` 只代表建议，`ActionCommitted` 表示进入最终响应，`ActionAccepted` 表示裁判回执为真，`EffectObserved` 才提供具体效果证据。只有真正提交的动作令个体进入动作等待态；本地资源冲突/校验拒绝不等待回执。

报告使用 Step/Plan/Mission/Strategy 作用域，父层复验自身目标后再完成，防止一次到位/攻击结束整个防守任务。内部取消只撤销未来控制权限，不删除待对账效果，也不宣称裁判挑战已经结束。代价是需要独立动作账本和任务会话状态，收益是避免误完成、重复消费与任务串用。

## D14：有限推进保留证据，等待状态有恢复条件

**状态：采纳，补充 D04/D09/D11；具体阈值可调。** 每个实例每回合上行/下行各一次普通转移，加提交阶段一次和最多一次紧急重规划；事件先进入有限 inbox/账本，状态推进受限时证据不得丢失。世界关键数据失效时降级，恢复后重验计划。

所有 Hold/Blocked/等待态定义唤醒条件、期限与重试策略。认知作业终止后保留发送回合墓碑，只有能区分新旧结果时才复用通道；内部取消的挑战由世界模型继续跟踪实际结束。代价是可能增加少量内部流程回合和受控存储，收益是避免事件递归、无期限等待与迟到结果污染。若测得简单 SOP 的状态推进延迟过大，优先合并纯本地准备步骤，并保留动作一次提交和预算上限。

## D15：接口契约单独维护，跨层状态不可写

**状态：采纳，细化 D01/D02/D12。** 新增 `INTERFACES.md` 作为函数签名、字段、所有权、错误和时序的唯一接口基线，DESIGN 保留行为与算法。新增共享 `decision/` 承载聚合只读视图的上下文，避免基础 domain 反向依赖具体世界实现。

各层由 runtime 调用并路由类型化输出，不持有彼此的可变对象。MissionSpec/MissionRecord/Assignment、Intent/Proposal/CommittedAction 明确分开；父链版本决定子建议是否有效。代价是类型数量增加，收益是减少隐式耦合与取消后的旧动作。

## D16：真实观测幂等归并，规划结果事务提交

**状态：采纳，细化 D11/D13。** A 阶段真实观测和上一轮效果一旦确认就保留；B/C 在草稿中规划。完整校验、编码及草稿提交回调完成后，D 阶段原子提交内部计划状态、预约与响应缓存。

这明确了“原子提交”不等于回滚真实世界：超时降级保留实际死亡/支出，丢弃未发送建议及其等待状态。本地幂等保证相同请求返回相同字节，不承诺网络或裁判恰好执行一次。代价是需要基线/草稿和提交前一致性检查。

## D17：预约单一写入者与批准后分配

**状态：采纳，细化 D05/D06。** ResourceCoordinator 是账本唯一写入入口，任务/战术只发租约申请；必要资源全部批准后才派发 Assignment/Intent。已有份额通过 UseLease 引用，不能重复扣款；已发送消费不能因任务取消而退款。

动作仲裁重算必需 claims，不能信任个体自行声明已占用控制者/金币。ValidatedBundle 使用受控构造，编码器只接受通过整包约束的集合。额外批准阶段增加少量接口，但使资金、人员、站位和认知通道使用具有同一口径。

## D18：明确回放确定性与实时预算的边界

**状态：采纳，澄清 D11/D12/D14。** 严格确定性比较使用固定工作量预算，或重放原先记录的截断决策；实时单调时钟截止仍用于响应可靠性，不宣称任意硬件负载下的降级输出完全相同。

同一回合 WorldView 冻结；新认知结果可作为显式任务输入，但预测仓更新延至下一回合 A 阶段。代价是共享预测最多延后一回合，收益是所有层能依据同一个稳定快照决策。

## D19：状态拓扑、行为算法、接口契约分别维护

**状态：采纳，细化 D04/D14/D15。** DESIGN 继续管理架构和允许边，新增 BEHAVIOR 集中定义全部 78 个状态的计算/输出/切换规则、A01–A12 算法、G01–G16 守卫及数值用例；INTERFACES 管理相应类型与调用。TransitionRuleId 和包含 guard 分项的 BehaviorTrace 使切换可以用回放解释。

不把所有处理器实现为单个大 match；公共守卫和纯计算可复用，状态私有上下文独立。代价是跨文档同步检查，收益是编码无需自行猜测“风险高”“应该返防”等抽象描述。行为表新增/删除边时同步 DESIGN，接口字段变化同步 INTERFACES；BH01 检查全状态和全边覆盖。

## D20：角色偏好服从约束优先的全队匹配

**状态：采纳，细化 D05/D06。** W1工程/W2经济只用于并列选择；两名工人可互替、并行施工、操控任何合法武器。P负责不可替代的挑战/宝藏，也可补防；个人物品不能虚构共享。候选先过滤工种、期限、资源和最低防守，再按保全/必要防御/临期兑现/发展/探索优先向量、效用及稳定序选择。

采用有界枚举和批准后分配，返防 ETA 包含未来工作终点到炮位的路径及流程开销。代价是初版偏保守，可能错过高收益冒险；收益是可预测、可验证，避免角色各自贪心导致漏防或抢资源。策略调整须通过同样本换边回放。

原任务因抢占取消后，撤退使用新建 Escape 任务的 owner 和安全目标，不能继续借失效挑战/采矿计划发移动。逃生可以只有移动，不强制拥有治疗物品；完成同时核对实际到位和当前风险。

## D21：明确可调基线与结果未知的处理规则

**状态：采纳，补充 D07/D09/D13/D18。** 规则常量、策略初值、未知裁判行为分别标 R/P/U。风险用有界窗口的整数近似及进入/退出滞回，默认三炮配兵、返防余量、认知耗时与成功率先验均为 P；版本化配置经过回放后才调优，不写成已验证最优值。

证据质量采用可解释分级并取宝藏各关键维度最低值，不把 LLM 自报置信度当事实。所有等待有唤醒条件和期限；结果未知保留消费/作业墓碑，允许可靠快照下无重复消费风险的新动作，禁止以“超时”退款或盲重发。代价是部分规则缺失时禁用能力、结果模糊时减少尝试；收益是行为具有明确恢复路径且可审计。对应验证为 BH03/BH04/BH11/BH14–BH18。

## D22：首阶段以标准库 HTTP 和垂直切片落地

**状态：采纳，阶段性实现决策。** 首阶段以 `serde/serde_json` 处理 JSON，标准库阻塞 TCP 提供有界 HTTP 服务；先贯通 `protocol → runtime → world → ai → command` 和独立测试，再按 B01–B10 逐步替换简化契约。此选择避免在比赛运行环境尚未确认时引入异步运行时，并使端到端响应可先验证。代价是当前没有完整路由/并发能力、结构化遥测和正式 IF02/IF03 代次事务；当官方环境或负载测试证明必要时，评估更换传输实现，但不改变层级边界。

## D23：未知建造区由外部掩码显式授权

**状态：被 D24 替代。** 在建造区未知期间，`BOT_BUILD_MASK_PATH` 曾按阵营显式授权武器坐标，并在没有配置时禁用自动建设。用户随后提供可生成完整区域的正式几何规则，因此不再需要坐标文件。

## D24：建造区域由观测基地和 2×2/4×4/6×6 规则生成

**状态：采纳，替代 D23，解决 U01。** 接口中的基地 `pos` 是 2×2 占地左上角；外扩一格的 4×4 减基地为武器环，外扩两格的 6×6 减内层 4×4 为围墙环。规则层从每回合观测基地生成区域并裁剪地图边界，策略不维护阵营坐标表。完整无裁剪区域分别有 12 个武器格和 20 个围墙格。动作仲裁强制武器/围墙分区；武器检查金币与总数，围墙检查工人个人背包的石头。仍需建设算法验证出口与操炮通路，几何合法不代表布局合理。

## D25：连续性异常降级并保留最后有效世界

**状态：采纳，部分落实 D14/D16。** 同一会话的地图尺寸或阵营相对最后有效快照发生突变时，世界进入 `Degraded`，不替换快照、不增加世界版本，也不基于旧位置返回缓存动作；一致的新帧可触发 `WorldRecovered` 并重新生成差分。生命周期和差分事件写入稳定递增 ID 的有界 `EventLog`，容量满时移除最旧记录，防止长期对局后无界增长。D26 已在其上增加按读者确认和截断检测；显式 `Closed`、关键事件截断恢复及完整路由仍留在 P2。

## D26：事件消费游标和 OwnerPath 使用完整链校验

**状态：采纳，部分落实 D04/D13/D15。** 每个层级读者独立保存下一个待消费事件 ID；poll 不改变状态，处理完成后以包含起止 cursor 的 receipt 确认。只有 receipt 起点仍匹配当前 cursor 才能推进，重复/迟到确认返回 `StaleReceipt`；最旧保留 ID 越过 cursor 时批次显式标记截断。战略 cursor 属于 `DecisionState` 草稿，只有响应编码和截止检查成功才随决策提交，避免失败规划吞掉事件。当前世界事件可由完整观测重验，信封路由和关键截断恢复另行实现。

任务、计划和意图分别使用独立 ID 与 generation。`OwnerPath` 强制 intent 必须有 plan，`ActiveOwners` 注册子节点时校验当前父链、generation 严格递增且同一 ID 不换父；提交校验从 mission 到 intent 逐级核对，父 generation 改变即可使整个旧链失效。代价是重建父任务时必须分配新的子 ID；收益是旧建议不能仅凭末级 generation 偶然匹配。D27 已将该路径接入简化动作等待和报告，D28 再将它前移到动作提案和响应编译；持久任务 assignment 仍待实现。

## D27：真实回执保留原 owner，状态推进要求 owner 当前有效

**状态：采纳，细化 D09/D13/D26；其中逐动作分配整条路径已由 D28/D29 替代。** 当时的垂直切片为每个实际被仲裁接受的动作在决策草稿中确定分配完整 mission/plan/intent 路径，等待记录和 Step 报告保存同一路径；攻击使用武器 actor 查询裁判结果，并以 controller 作为 reporter。角色开始替代工作时撤销旧 mission 活动登记。

对账先根据原 actor、发送回合和当前观测形成事实报告，无论 owner 是否仍有效；随后只有 `ActiveOwners::is_current` 通过，报告才能改变角色、任务和战术状态。因此取消或替代不能抹掉已经发生的裁判回执，迟到结果也不能推进新任务。owner 分配失败使整份决策草稿返回错误并输出空响应，不提交部分等待状态。当前每动作一条任务链是过渡模型；持久任务 DAG 落地后由 assignment 复用 mission/plan，只为新步骤生成 intent，并在提案和最终提交边界复验。

## D28：动作提案携带 owner，并在响应编译前复验

**状态：采纳，细化 D17/D26/D27；owner 创建粒度由 D29 继续细化。** 简化 `ActionProposal` 在进入仲裁前携带 actor、action 和完整 OwnerPath。候选 owner 先在决策草稿中注册；本地动作或冲突校验失败时只撤销该候选，角色原工作不变。提案初次获准后才替换角色 owner；攻击以 controller 作为工作归属，以武器作为协议 actor。

`Arbiter::finish` 在生成 RoleCommand 和 accepted 集合前再次校验完整父链，期间已失效的提案不会编码，也不会创建 PendingAction。最终集合封装在 `ArbitrationResult` 内，只允许个体提交记录读取；个体层复用其中的 owner，不再另建路径。这使等待、回执与仲裁授权拥有同一身份，并保留 Session 草稿的原子提交性质。D29 已让同类工作跨动作复用 mission/plan；ProposalId、TurnStamp、claims、ExpectedEffect、原子组及正式 Reservation/ValidatedAction 仍留给 P2/P3。

## D29：工作 assignment 复用 mission/plan，动作只替换 intent

**状态：采纳，细化 D26–D28；工作族键由 D30 细化为稳定目标键。** 当时的垂直切片按 reporter 和 `Construction/Economy/Challenge/Defense` 工作族保存简化 assignment。相同工作族的后续动作沿用 mission/plan，并为每个候选创建新的 intent；只有提案获准才停用上一 intent。工作族改变时创建新 mission/plan/intent，并在获准后停用旧 assignment；本地拒绝只停用候选节点，原工作仍有效。攻击 assignment 归 controller，协议 actor 仍是武器。

`ActiveOwners` 为三层注册都保留 generation 与 active 标志。停用节点不删除代次记录，同 ID 恢复必须使用更高 generation；停用 plan 只使该 plan 及其 intent 失效，停用 mission 使完整子链失效。分配器和 assignment 都位于 `DecisionState` 草稿，失败或丢弃的决策不会推进持久 ID。D30 已消除同一工作族不同建造点或武器共用 plan 的问题；逐步 intent 和完整父链校验规则保持不变。

## D30：MissionSpec 使用稳定目标键决定 assignment 身份

**状态：采纳，细化 D29 和目标 IF07。** 当前不可变 `MissionSpec` 由 mission kind 与 objective key 组成。经济循环与挑战会话使用固定目标键，建设任务使用建造格，守备任务使用武器 ID。reporter 只有在完整 spec 相同时复用 mission/plan；kind 或 objective 变化时创建新 assignment，并在新提案获准后停用旧父链。每一步仍创建独立 intent，攻击的 reporter/actor 规则不变。

构造器绑定合法 kind/objective 组合，避免调用方构造“建设任务指向武器”等无效身份。目标键来自已选 BuildPlan 或防守武器，保证接近目标与执行动作属于同一任务。代价是经济和挑战暂为会话级粗目标，spec 也尚未包含 owner、goal、依赖、期限、优先级和租约；这些字段随持久任务 DAG 增补，不改变以稳定目标键判断 assignment 延续性的规则。

## D31：任务记录由 MissionRegistry 私有持有并传播依赖结果

**状态：采纳，细化 D05/D26/D29/D30 和 IF07。** `MissionRegistry` 以 MissionId 保存私有 MissionRecord，记录简化 spec、assignment owner、assignee、MissionState 和前置 MissionId；外部只能取得克隆的 MissionView。活动任务另按 assignee 索引，完整 spec 相同才复用父路径。首次动作获准后记录经过 Ready → Assigned → Executing；Blocked 任务在后续动作获准时恢复 Executing。owner 与 registry 错误统一进入 `DecisionError`，任一失败都丢弃整个 Session 决策草稿。

注册拒绝未知、自身或已不可用的前置；任务成功后统一扫描并唤醒所有前置均成功的 Proposed 记录，取消则按 MissionId 稳定顺序传播到依赖子树。成功返回 MissionCompletion，取消/替换返回 MissionCancellation 集合，调用方必须在同一草稿中停用对应 ActiveOwners 父链，因此后代不能继续输出动作。代价是每次成功或取消当前采用小规模全表扫描，但比赛角色和任务规模有限、结果确定；任务规模增长后可增加反向依赖索引。自动 MissionFactory、goal/deadline/priority/lease、失败传播策略和完成证据仍待实现。

## D32：MissionSpec 是任务身份和调度约束的唯一不可变契约

**状态：采纳，细化 D30/D31 和 IF07。** MissionSpec 从 `{kind, objective}` 扩展为同时包含类型化 GoalPredicate、前置 MissionId 集合、required capabilities、MissionDeadline、Q0–Q4 PriorityClass、Interruptibility 和 RetryPolicy。建设任务的 goal 同时保存建筑类型与坐标，避免同一地块更换建筑类型时误复用任务。MissionRegistry 不再接收单独 dependencies 参数，所有 DAG 守卫和传播只读取 spec 中的唯一集合。

assignment 复用比较完整 MissionSpec，因此目标、依赖、期限、优先级或执行策略任一变化都会创建新父路径；这避免旧计划继承已经改变的授权。MissionDeadline 明确 action submission、effect observation 和 internal planning 三种期限，并按 inclusive 标志实现 G06 边界。代价是 MissionSpec 不再 Copy，调用点必须显式借用或克隆；任务规模很小，契约克隆成本可控。当前四类构造器提供策略默认值，但实时 MissionFactory 尚未生成依赖/deadline，也未执行 goal 证据求值、能力过滤和期限转移。

## D33：任务终态只能由带来源回合的目标证据或期限守卫产生

**状态：采纳，落实 D09/D31/D32 的基础 G05/G06 闭环。** 每轮决策开始时，MissionRegistry 用冻结观测和 EventLog 记录求值 GoalPredicate。完成证据使用类型化实体或事件引用并保存 observed_round；建设要求目标格出现指定存活建筑，挑战结束和守备窗口分别要求 ChallengeEnded 与进入白昼事件，AllOf 合并全部子证据。共享金币变化和单次动作合法回执不足以证明完整经济循环，因此该目标继续 Pending。

EffectObservation 与 InternalPlanning 要求完成证据落在 deadline 窗口内，迟到观测不能把 Expired 改回 Succeeded。ActionSubmission 只由与目标匹配的 Sell、Build、SubmitAnswer 或对应武器 Attack 形成 checkpoint；移动等准备动作不满足提交期限，按时目标动作则允许效果在后续回合对账。无有效提交的根任务到期进入 Expired，依赖后代按稳定顺序 Cancelled，所有 resolution 在同一决策草稿中失效 ActiveOwners。代价是当前 goal evaluator 只覆盖四类基础谓词，经济跨步证据和挑战代次仍需后续 checkpoint 模型。

## D34：能力准入在 owner 分配之前统一执行

**状态：采纳，落实 D20 和 BH05 的基础能力过滤。** 当前角色能力只由观测中的 roleType 和可靠生命值生成：worker 提供 Gather、Sell、Build、OperateWeapon，pioneer 提供 SolveChallenge、OperateWeapon。MissionSpec.required_capabilities 必须是该集合的子集；角色不存在、确认死亡、生命未知或缺少具体能力分别形成类型化 CapabilityRejection。攻击的任务 assignee 是 controller，能力检查不把武器 actor 当作角色。

`propose_owned` 在创建 mission/plan/intent 及替换旧工作前执行准入，不匹配候选按正常不可行结果返回，不消耗 owner ID，也不改变现有 assignment。动作仲裁仍负责动作级工种、站位、阶段和资源复验，能力准入不替代最终合法性检查。代价是当前能力表仍是两类角色的静态规则；未来购买物品、冷却、任务租约和临时不可用性进入 AvailabilityView，不应膨胀 MissionCapability。

## D35：经济任务以跨步效果证据完成

**状态：采纳，补全 D33 的 `EconomyCycleCompleted` checkpoint。** 任务记录在实际提交 Collect 时保存物品种类、执行角色的提交前数量和预计效果回合；下一帧必须同时出现该物品数量增加与同角色 `InventoryChanged` 事件，才记录本任务的采集证据。实际提交 Sell 时保存出售后预期余量和提交前团队金币；只有任务已有采集证据，且下一帧同时出现指定物品减少、同角色背包事件，以及从保存基线开始的正向 `GoldChanged`，才完成经济循环。

单次合法回执、任务开始前已有库存或无归属的金币变化均不足以完成目标。checkpoint 与证据由私有 MissionRecord 持有，MissionView 只公开稳定证据副本。共享金币缺少交易流水时，并发消费可能抵消出售收入；当前宁可保持 Pending，也不虚构收益归属。后续 ResourceCoordinator 或动作效果账本可在不改变完成语义的前提下分解净变化。

## D36：自动期限在首次 assignment 冻结

**状态：采纳，落实 D32/D33 的实时期限生成。** 经济与建设任务使用当日最后回合作为 inclusive ActionSubmission deadline；挑战取该边界与 `首次候选回合 + timeoutRounds - ANSWER_MARGIN_ROUNDS` 的较早者；整夜防守使用下一白昼首回合的 inclusive EffectObservation deadline。这样及时提交的 Sell、Build 或 SubmitAnswer 可以等待后续效果，而未提交的工作会在窗口结束后 Expired。

相对 deadline 只在自动任务首次分配时计算。同 assignee 的活动任务若除 deadline 外契约完全相同，后续步骤复用已冻结的完整 spec；显式调用方改变 deadline 仍按 D32 建立新 assignment。当前回合已过期的提案在 owner 分配前拒绝，不消耗 ID 或替换现有工作。`timeoutRounds` 的裁判起算点属 U10；当前从候选首次准入保守计时，代价是可能提前放弃，收益是不让期限随重规划无限后移。

## 变更规则

为新决策分配递增 D 编号，保留旧决策并标记“被 Dxx 替代”，说明触发证据、兼容影响和验证结果；不要抹去仍影响现有实现的假设。规则缺口获得证据后同时更新 DESIGN 的 U 条目及 PLAN 的验收状态。
