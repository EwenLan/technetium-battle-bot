# 实施步骤与计划

> 更新：2026-09-20。已交付首个可运行切片；P0–P7 与 B01–B10 的完整验收均尚未通过。采用依赖驱动的里程碑，不在运行环境和裁判接口未确认时承诺日程。

## 1. 当前状态与执行原则

- [x] 完整阅读任务书、接口文档、请求/响应示例，检查 Demo 核心代码。
- [x] 制定分层 AI、FSM、事件、上报、世界模型、动作仲裁和认知作业设计。
- [x] 建立设计、交接、上下文、决策和本计划，并更新 `AGENTS.md` 文档导航。
- [x] 细化各层实现工作、状态行为/转移表、事件与上报目录、挑战/宝藏子流程及必测轨迹。
- [x] 建立 INTERFACES 的 IF01–IF16 接口契约、类型/所有权/事务规则及 API01–API15 待实现验收规格。
- [x] 建立 BEHAVIOR：78 个状态处理规格、三角色/三武器规则、A01–A12 算法、G01–G16 守卫、数值算例及 BH01–BH18 待实现验收。
- [x] 首阶段实现可启动的 HTTP 服务、基础世界差分、分层策略切片和动作仲裁；通过本地协议/行为测试及 HTTP 冒烟。
- [ ] 完成比赛服务与五层 AI 的全部契约和官方环境验收。

实现以 [DESIGN.md](DESIGN.md) 为架构基线，以 [INTERFACES.md](INTERFACES.md) 为模块契约基线，以 [BEHAVIOR.md](BEHAVIOR.md) 为逐状态算法基线；重大调整先更新 [DECISION.md](DECISION.md)。每阶段交付可评审的小步改动，更新 [HANDOFF.md](HANDOFF.md) 的进度、已验证内容和下一步。首阶段增加 `serde/serde_json`、源码、测试和 `run.sh`；下列未勾选条目需按完整验收标准继续实现。

### 首阶段已完成的具体子项

- [x] `bash run.sh <port>` 可本地启动；POST JSON 返回完整 `roleCommandMap/prompt/executeCmd`，同轮相同请求返回相同字节。
- [x] 解析必要字段并拒绝重复 JSON key；缺失可选字段保留未知，提供合法协议 fixture。
- [x] 世界快照差分、基础事件、普通敌人失踪与全图可见建筑移除记忆；地图八方向邻接与 A*、基地完整 2×2 距离。
- [x] 基础昼夜/返防/紧急态选择、工人采售与条件建炮、开拓者接题/下一回合回答、夜间单炮基本操控。
- [x] 对首阶段支持的动作执行角色、站位、冷却、目标数量/锥角、金币与占位检查；未支持动作不发送。
- [x] 已提交动作进入等待；下轮按合法性回执生成 Step 报告，拒绝/未知映射到简化任务与战术恢复状态，不把合法回执当成效果确认。
- [x] 同一任务点的多格 `zones` 合并交互站位与接取判断；任务邻域的正式裁判语义仍属 U03。
- [x] 世界 `ColdStart/Ready/Degraded` 接入会话；连续性异常保留最后有效快照并空响应，一致新帧恢复。
- [x] 当前轮世界事件写入带稳定 ID/来源回合的有界日志；四层读者具有独立 cursor/poll/ack、截断与陈旧 receipt 检测，战略 cursor 随决策草稿事务提交。
- [x] 实现 `OwnerPath/ActiveOwners` 的 mission→plan→intent 完整父链、严格 generation 推进和旧父链失效检查。
- [x] 已接受动作、等待记录和 Step 报告携带同一 OwnerPath；替代工作撤销旧 mission，迟到报告保留事实但不推进新工作。
- [x] 简化 `ActionProposal` 在仲裁前携带 actor/action/owner；初次及响应编译前复验完整父链，本地拒绝不替换原工作，等待记录复用已批准 owner。
- [x] 建立按角色和简化 MissionSpec 复用的 assignment；建造格与武器 ID 作为稳定目标键，完整 spec 相同时保持 mission/plan，每个新步骤分配 intent，目标替换和拒绝按层失效。
- [x] 建立持久 MissionRegistry 与只读 MissionView；实现 Proposed/Ready/Assigned/Executing/Blocked/Succeeded/Cancelled 基础转移、前置成功解锁、取消子树传播和活动任务替换。
- [x] 连续帧验证 `DayDevelop → PrepareNight → NightDefend → DayDevelop`，并覆盖紧急态退出滞回。
- [x] 协议校验覆盖我方/敌方/机器人、任务点和基地完整 2×2 footprint，越界帧不进入世界模型。
- [ ] 完成正式四层/世界 FSM、事件上报闭环、全部 12 类动作、认知作业与新闻/宝藏算法。

## 2. 阶段与验收

### P0：规则、协议与环境基线

依赖：无。优先完成，未决项编号见 DESIGN 第 12 节。

- [ ] 将字段必选/可选、动作必填参数、规则常量整理成可执行测试清单。
- [ ] 从 `request.txt` 制作兼容样例；将 `response.txt` 的动作变体分别整理成有效 JSON，保留原始资料不改写。
- [x] 确认 U01：基地 2×2、武器环为 4×4 减基地、围墙环为 6×6 减 4×4；从观测基地动态生成并测试。
- [ ] 确认 U02 回合边界、U03 任务邻域、U04 HTTP 契约、U09 运行环境；取得官方依据后记录版本。
- [ ] 补齐 U05–U08 的待测用例与暂定行为，不把 Demo 当裁判。
- [ ] 确定 Rust 工具链、目标平台、依赖版本和离线构建/运行方式。

验收：每项规则可追溯到资料或明确的待验证假设；至少包含两阵营地图用例、12 类动作样例和边界回合样例。U01 已有用户确认和几何测试，但完整 P0 仍受其余未决项阻塞。

### P1：协议服务与回合运行时

依赖：P0 的基本协议与环境结论。

- [x] 创建 `lib.rs`、`transport/`、`protocol/`、`domain/`、`runtime/` 和 `run.sh`。
- [ ] 实现 `bash run.sh <port>` 启动，监听 `0.0.0.0`，支持确认后的 POST 路由。
- [x] 类型化解析；区分必要字段错误与可选字段未知，兼容未来额外字段。
- [x] 实现串行回合状态、同回合相同请求缓存、过期/冲突请求处理与完整空响应。
- [ ] 实现决策预算、保守兜底、generation 提交检查及基础结构化日志。
- [ ] 按 IF01–IF03 区分观测幂等更新与规划事务提交，规范 RequestKey；编码成功及草稿回调通过后才提交响应缓存。

验收：真实 HTTP 冒烟测试成功；异常输入不会令进程退出；重复请求返回相同响应且不推进状态；慢决策在本地硬预算内降级，且不能事后提交旧结果。验证空动作、空 prompt、空 executeCmd 的裁判语义。

### P2：世界模型、FSM 与事件闭环

依赖：P1。

- [ ] 实现实体、地图占用、基地 2×2、任务点合并、资源/价格/背包索引。
- [ ] 建立观测事实、历史记忆、预测三种数据及显式来源/有效期。
- [ ] 实现世界差分、昼夜时钟、死亡/复活、冷却和新闻事件。
- [ ] 实现四层 enum 状态机的共用转移契约、分阶段投递与上报汇总。
- [ ] 在已接入依赖集合和基础传播的 MissionRegistry 上补齐自动任务分解、goal、期限、优先级、能力、租约、checkpoint、progress、完成证据、超时、去重和有限重规划；当前自动策略创建的任务尚未使用依赖。
- [ ] 在已有世界降级和按层 cursor/poll/ack 基础上，实现实例路由 inbox、关键截断恢复、guard 与转移预算；事件证据保存后才推进消费游标。
- [ ] 按 DESIGN 第 7 节实现类型化事件/报告，覆盖生产者、payload、路由、scope、优先级和有效期。
- [ ] 建立 `decision/` 共用上下文，完成 IF04/IF05/IF10/IF11 的信封、过滤、路由和 FSM 驱动；把已实现的 OwnerPath 校验放入所有提交边界，禁止跨层写状态。

验收：连续观测可驱动 `DayDevelop → PrepareNight → NightDefend → DayDevelop`；未知敌人消失不被认定死亡；机器人清晨清除不被认定击杀；重复新闻不重复消费；相同回放生成相同事件与转移轨迹。

### P3：个体动作、任务分配与白天经济

依赖：P2；建造区域 U01 已解决。

- [ ] 将已实现的简化 `ActionProposal → RoleCommand` 扩展为 `ActionProposal → Reservation → ValidatedAction → RoleCommand`，补齐 ID/stamp/claims/expected/group。
- [ ] 实现角色/武器独占、金币/物品数量账本、目标格预约与取消对账。
- [ ] 实现八方向 A*、可交互终点集合、争同格/互换拒绝、阻塞上报。
- [ ] 建立采集、出售、购买、建造、升级、维修的任务依赖与能力过滤。
- [ ] 使用三角色的小规模枚举分配任务；配置初始经济/防御策略和返防时间预算。
- [ ] 按 DESIGN 6.2–6.4 实现任务生命周期、战术/个体状态表；区分本地建议拒绝、动作提交与效果确认。
- [ ] 落地 IF06–IF09、IF12–IF14：先申请/批准租约再分配，禁止重复扣已有份额，整包校验后才能编码。

验收：完成“采集→出售→购买→使用”的观测闭环；同回合两名工人不能超额花钱或抢占同一格；建造总武器不超过 3；布局保留可达出口；未经确认的效果不会直接写入金币/背包事实。

### P4：夜间防守与战略抢占

依赖：P3；精准弹道依赖 U06。

- [ ] 建立威胁图与基地风险指标，提前将角色分配到武器操作位。
- [ ] 实现三种武器的候选落点、等级目标数、冷却、射程、锥角与伤害估计。
- [ ] 联合分配控制者和火力，避免同一角色操控两炮或操炮时另行移动。
- [ ] 实现 `Emergency`、退出滞回、任务抢占与重分配；支持角色死亡及复活后恢复。
- [ ] 实现受规则约束的升级回血和应急消耗品策略。

验收：覆盖至少一个完整昼夜轨迹、返防遇阻、火箭冷却、控制者死亡和升级回血场景；不存在本地可检测的非法动作。能在目标时限内完成三武器联合规划；未验证的弹道行为明确标注，不以自建模拟器结果替代裁判证据。

### P5：自进化任务与认知作业

依赖：P2/P3 的任务会话与动作校验；集成时须通过 P4 防守回归。

- [ ] 建立 `prompt/llmResp` 与 `executeCmd/lastCmdResult` 的跨回合作业状态机。
- [ ] 实现每日三次额度、任务期免额度、同通道单在途作业、超时与过期结果隔离。
- [ ] 建立接取、站位保持、SOP 检索、命令执行、答案提交、反馈修正与学习闭环。
- [ ] 解析沙盒退出码、超时、异常、截断；检查模型输出结构及命令任务范围。
- [ ] 支持截止前提交部分答案，危险撤离时明确终止任务并取消关联作业。
- [ ] 实现 DESIGN 10.3–10.5 的挑战与认知作业子状态机；验证内部取消不等于裁判会话结束，旧作业墓碑阻止结果串用。
- [ ] 按 IF15 实现认知请求/作业/结果/SOP 契约；额度与通道统一预约，假设验证后在下一回合冻结前进入世界预测仓。

验收：模拟下一回合返回、空结果、额度超限、任务结束后迟到、跨日、重复请求；均不得把结果应用到错误任务。等待认知结果期间工人继续采集或防守，HTTP 不等待模型或沙盒运行。

### P6：新闻、长上下文与收益优化

依赖：P5。

- [ ] 官方消息结构化为有证据的矿产影响与生效区间，结合实时价格调整任务效用。
- [ ] 跨日存储民间传闻、矛盾线索、宝藏候选、开放窗口与献祭物品多重集。
- [ ] 按资源、时间、置信度和返防成本选择探索，处理宝藏结果 0–4 的不同语义。
- [ ] 用版本化 SOP 提升重复任务效率，记录失败和适用前提。
- [ ] 在稳定基线之上评估召唤令、敌方施压与终盘策略，默认可关闭。
- [ ] 实现新闻/宝藏状态表的证据验证、等待开放和失败后重新评估；确认用品消耗前不得重发献祭。

验收：传闻跨日不丢失；地点错误/未开放不被错误区分；失败献祭的消耗可对账；策略变化通过同样本换边比较而非单局印象判断。

### P7：比赛回归、性能与交付

依赖：P1–P6，所有影响实际参赛的未决事项已解决或有明确禁用范围。

- [ ] 构建连续回合回放集，覆盖双方阵营、不同波次、不同新闻与任务、死亡/基地被毁和终盘。
- [ ] 在官方或已确认环境执行 1300 回合运行及两半场换边，统计异常、指令失败、积分、存活和耗时。
- [ ] 检查同输入确定性、内存/日志上限、依赖锁定、离线启动和端口配置。
- [ ] 按 IF16 分别验证确定工作量回放与实时截止降级，核对 API01–API15 契约场景，记录完整输入/配置/预算模式。
- [ ] 对比无新闻、无 SOP、固定火力等基线，保留真正改善稳定性或得分的优化。
- [ ] 更新全部交接文档，记录环境、命令、结果、剩余限制及配置版本。

验收：格式检查、静态检查和所有已建立测试通过；正式轨迹中无协议异常；响应满足官方时限。性能报告写明硬件、样本数量、p50/p95/max，不将目标值写成测量结果。

## 3. 关键测试矩阵

| 领域 | 代表用例 |
| --- | --- |
| 协议 | 缺少可选字段、未知商品、错误必填字段、攻击武器 key、无重复角色动作 |
| 世界 | 基地左上角占地、敌人视野消失、两格任务点、矿点同坐标刷新 |
| 时间 | 1/70/71/130/131、最终回合、复活、任务超时、日额度重置 |
| 预约 | 同格、互换、金币不足、控制者冲突、死亡与取消释放 |
| FSM | 抢占、阻塞重试上限、紧急滞回、任务 generation、事件去重 |
| 战斗 | 多目标数量与角度、穿透耗能、溅射叠加、火箭冷却、回合末伤害 |
| 认知 | 跨回合结果、沙盒截断、迟到、SOP 不适用、错误答案、失败献祭 |
| 服务 | 重复/乱序/冲突请求、预算耗尽、日志失败、长期运行、换边会话 |

按改动执行 `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test --all-targets` 与必要的 HTTP/回放测试。首阶段已有独立测试和本地 HTTP 冒烟，但不能据此认定完整比赛能力或官方兼容性已验证。

状态机逐项验收采用 DESIGN 11.1 的 FSM01–FSM16 轨迹：每条转移检查 guard 正/反例、重复事件、取消/死亡/超时优先级，核对状态、输出建议、资源释放和上报作用域；完整轨迹验收目前均未执行。

模块接缝采用 INTERFACES 第 15 节的 API01–API15，重点检查真实观测不可回滚、批准租约与分配握手、完整父链取消、编码前验证及旧结果隔离；首阶段仅有部分简化场景测试，完整 API 契约测试尚未实现。

## 4. 可直接开始编码的行为工作包

这些工作包细化 P0–P7，不另开一条跳过协议和规则确认的路线。顺序由依赖决定；表中路径是完整目标，部分目录和简化代码已创建，并不表示工作包通过验收。每包先准备固定输入/预期输出，再实现纯计算和 FSM handler，最后接入 runtime 与回放。状态处理器按状态族拆文件，不能把 78 个状态放入一个巨型函数。

| 工作包/阶段 | 依赖 | 实现位置与具体交付 | 验收门槛 |
| --- | --- | --- | --- |
| B01 参数与角色 / P0–P2 | P0规则资料、P1值类型 | `rules/constants.rs`、`decision/policy/`、`domain/roles.rs`；区分R/P/U，校验配置，稳定W1/W2/P；实现 INTERFACES 2.4 值对象 | BH05；未知规则不补零；角色ID换阵营仍正确；记录配置版本 |
| B02 统一守卫与世界 / P2 | B01、P1事务 | `fsm/`、`world/lifecycle/`；A01/A02/A12、W四态、完整OwnerPath、分阶段预算、TransitionRuleId/BehaviorTrace | BH01的W部分、BH02；每条边正反例、同轮多事件优先级、重复请求不推进 |
| B03 几何与风险 / P2–P3 | B01、B02、世界实体索引 | `rules/geometry/`、`ai/tactics/navigation/`、`decision/risk/`；A03/A11、风险向上取整、ETA、稳定A*、stall和目标级重试计数 | BH03/BH04/BH12；返防等号、角色risk819、两次真实阻塞、预算不足Incomplete |
| B04 动作与个体 / P3 | B02、B03、IF12–IF14 | `command/tracker/`、`ai/individual/states/`；A10、I七态；提交回执、效果确认、消费Unknown和失效清理 | BH01的I部分、BH11；本地未选中不等待，死亡后旧效果可对账 |
| B05 候选与任务 / P3 | B01–B04 | `ai/mission/candidates/`、`assignment/`、`states/`；A04/A05、M十态、DAG、资源批准握手、分数分项和稳定并列规则 | BH01的M部分、BH06/BH07；金币25冲突、采售周期完整、失败不是无限新建任务 |
| B06 战术与建设 / P3 | B03–B05；U01 已解决 | `ai/tactics/states/`、`ai/mission/construction/`；T十一态、A06、交互站位、工程/采购/升级/修复节点 | BH01的T部分、BH08/BH13；Hold可唤醒、普通Blocked放人、挑战不自动离开、出口可达 |
| B07 战略与防守 / P4 | B05/B06；弹道依赖U06 | `ai/strategy/states/`、`ai/tactics/defense/`、`fire/`；S七态、A07/A09、返防匹配、联合火力、Emergency滞回、Endgame | BH01的S部分、BH03/BH04/BH09/BH10；完整昼夜、补药占槽、冷却Unknown、最终回合动作 |
| B08 挑战与作业 / P5 | B02/B04–B07、IF15 | `ai/mission/challenge/`、`cognition/jobs/`、`sop/`；A08、C十四态/J八态；NextStep、部分答案、任务取消/通道墓碑 | BH01的C/J部分、BH14/BH15；Sent收结果不丢、未知期限策略、工人并行执行、重试有上限 |
| B09 新闻与宝藏 / P6 | B05/B08 | `cognition/news/`、`treasure/`；N六态/B十一态、证据分级、相对日期、个人物品多重集、全场尝试上限 | BH01的N/B部分、BH16/BH17；独立线索、码2歧义、已空撤销所有候选、失败消费不退款 |
| B10 集成与调优 / P7 | B01–B09 | `tests/fixtures/behavior/`、独立行为/轨迹测试、回放报告；覆盖所有允许边与关键跨层链，参数变化同样本比较 | BH01–BH18及FSM/API全矩阵；固定工作预算确定性、双方阵营、1300轮、官方环境性能测量 |

- [ ] B01–B03：配置、守卫、世界、风险和寻路已实现并验证。
- [ ] B04–B06：个体、任务、战术及白天经济闭环已实现并验证。
- [ ] B07：角色返防和三炮联合防守已实现并验证。
- [ ] B08–B09：挑战、认知、新闻和宝藏闭环已实现并验证。
- [ ] B10：状态/接口/行为验收与实战回放通过，默认参数有测量依据。

每个状态的最低测试集：正常进入与输出、每条允许边的 guard true/false/Unknown、等待解除或终态幂等、取消/期限/死亡（适用时）、本地拒绝与实际提交（适用时）。全局边可共享参数化 fixture，但不能只测正常路径。每条轨迹检查状态、动作/作业、资源、上报和证据五类结果，不以文本日志包含状态名代替行为断言。

预期逐轮轨迹至少覆盖：工人建炮→采售→返防→开火→清晨恢复；控制者死亡→替补→实际复活再派遣；P接题→工具→有证据答案→结束/SOP；P紧急离场→旧LLM结果隔离；传闻→采购→等窗口→失败消费→有新证据才再评估。使用 BEHAVIOR 第 9 节算例建立小 fixture，再组合成长轨迹。

实现包完成后执行 AGENTS 检查并提交/推送，更新 PLAN 完成项、HANDOFF 结果、AGENT_CONTEXT 事实；参数/边/接口变化同步 BEHAVIOR、DESIGN、INTERFACES 和 DECISION。目前 B01–B10 均只有局部能力、未达到各自验收门槛；BH 全矩阵尚未完成，已有少量边界回归测试不等于工作包通过。
